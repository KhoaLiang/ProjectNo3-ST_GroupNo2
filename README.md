## The `IDE` folder is where our group use to store all the .side files for Selenium IDE (level 0 testing).

## The `project` folder is where our group use to store all the test script files for level 1 testing.